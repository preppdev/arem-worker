runpod>=1.6.0
# torch+torchvision must be cu128+ for sm_120 (Blackwell) kernel support.
# cu126 wheels DON'T include sm_120 even though CUDA 12.6 supports it
# in principle — PyTorch's pre-built wheels only ship sm_120 kernels in
# cu128+ builds. PyTorch's own warning text on the cu126 build says
# verbatim: "Please install PyTorch with a following CUDA
# configurations: 12.8 13.0".
torch==2.10.0+cu128
torchvision==0.25.0+cu128
--extra-index-url https://download.pytorch.org/whl/cu128
numpy>=1.26
opencv-python-headless>=4.10
rawpy>=0.21
tifffile>=2024.7
Pillow>=10.4
exifread>=3.0
piexif>=1.1.3
einops>=0.8
lensfunpy>=1.16
