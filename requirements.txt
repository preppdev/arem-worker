runpod>=1.6.0
# torch+torchvision pinned to CUDA 12.6 (sm_120 Blackwell support — see
# Dockerfile FROM comment). Matches the local-3090 conda env so the same
# Stage 1/2 checkpoints load byte-identical on either runtime.
torch==2.10.0+cu126
torchvision==0.25.0+cu126
--extra-index-url https://download.pytorch.org/whl/cu126
numpy>=1.26
opencv-python-headless>=4.10
rawpy>=0.21
tifffile>=2024.7
Pillow>=10.4
exifread>=3.0
piexif>=1.1.3
einops>=0.8
lensfunpy>=1.16
